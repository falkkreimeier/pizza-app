import styled from "styled-components/macro";

function Logo() {
  return (
    <Wrapper>
      <Image></Image>
    </Wrapper>
  );
}

const Wrapper = styled.section``;

const Image = styled.img``;

export default Logo;
